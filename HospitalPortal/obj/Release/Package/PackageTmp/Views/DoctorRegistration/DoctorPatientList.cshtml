@model HospitalPortal.Models.ViewModels.PatientDTO
@{
    ViewBag.Title = "PatientList";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Patient List</h2>
@using (Html.BeginForm())
{
    <div class="row" style="margin-bottom:3%;">
        <div class="col-md-4">
            Search By Patient Name
        </div>
        <div class="col-md-3" style="margin-left: -13%;">
            <input type="text" name="term" id="term" />
        </div>
        <div class="col-md-3">
            <input type="submit" value="Filter" class="btn btn-primary" />
        </div>
    </div>
}



<table class="table table-bordered">
   <thead>
       <tr>
           <th>Patient Name</th>
           <th>EmailId</th>
           <th>Mobile No</th>
           <th>Location</th>
       </tr>
   </thead>
    <tbody>
        @foreach(var item in Model.Patient)
        {
            <tr>
                <td>@item.PatientName</td>
                <td>@item.EmailId</td>
                <td>@item.MobileNumber</td>
                <td>@item.Location</td>
            </tr>
        }
    </tbody>
</table>
@*<nav aria-label="Page navigation example">
    <ul class="pagination"> 
        @for (int i = 1; i <= Model.TotalPages; i++)
        {
            if (i == Model.PageNumber)
            {
                <li class="page-item active"><a class="page-link" href="/DoctorRegistration/DoctorPatientList?pageNumber=@i">@i</a></li>

            }
            else
            {
                <li class="page-item"><a class="page-link" href="/DoctorRegistration/DoctorPatientList?pageNumber=@i">@i</a></li>

            }
        } 
    </ul>
</nav>*@


